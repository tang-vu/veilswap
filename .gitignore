# Dependencies
node_modules/

# Hardhat
artifacts/
cache/
types/

# Environment — never commit secrets
.env
.env.local
.env.*.local

# Build outputs
dist/
app/dist/
app/node_modules/

# Rendered demo video + its narration/caption sources. Kept out of git by
# default because they are large binaries; host the mp4 rather than commit it.
demo-video/

# Editor/OS noise
.DS_Store
Thumbs.db
